{
  "name": "cypress-best-practices-module-workshop",
  "version": "1.0.0",
  "scripts": {
    "cy:open": "./node_modules/.bin/cypress open",
    "cy:run": "./node_modules/.bin/cypress run",
    "postinstall": "git submodule update --init --recursive --remote && cd trelloapp && npm install -s",
    "start": "cd trelloapp && npm start"
  },
  "keywords": [
    "cypress"
  ],
  "author": "Filip Hric (https://filiphric.com/)",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.11.12",
    "cypress": "^12.7.0",
    "cypress-plugin-steps": "^1.1.0",
    "typescript": "^4.9.4"
  }
}
